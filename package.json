{
  "scripts": {
    "dev": "browserify -d src/game-of-life.js > dist/main.js --verbose",
    "prod": "browserify src/game-of-life.js -p [ minifyify --no-map --uglify [ --mangle [ 'toplevel' ] ] ] > dist/main.js"
  },
  "dependencies": {
    "browserify": "^16.3.0",
    "minifyify": "^7.3.5"
  }
}
